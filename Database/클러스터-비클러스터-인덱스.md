# 클러스터 인덱스 (Clustered Index)

- B-Tree 구조를 가지는 가장 일반적인 인덱스 종류
- Leaf 노드에서 인덱스는 항상 정렬된 상태를 유지하며, 인덱스에 실제 데이터를 함께 저장하고 있다.
- 인덱스를 기준으로 실제 테이블 데이터가 항상 정렬된 상태를 유지한다고 이해할 수 있다.
- 테이블 자체를 인덱스로 만드는 형태이므로, 테이블당 하나의 클러스터형 인덱스를 설정할 수 있다.
- 인덱스 기준으로 정렬되므로 인덱스로 가장 적합한 컬럼 하나만을 클러스터 인덱스로 지정하며, 대표적인 것이 PK이다.
- 인덱스 기준으로 데이터가 함께 정렬된 상태를 유지하므로 검색 속도가 빠르다.
- INSERT, UPDATE, DELETE 할 때마다 전체 데이터 페이지 정렬이 필요하므로 속도가 느리다.

# 비클러스터 인덱스 (Non-Clustered Index)

- 인덱스 페이지를 별도로 관리하며, 실제 데이터가 저장된 Heap 영역과는 분리된 구조이다.
- Leaf 노드에서 인덱스는 RID(RowID)를 가지며, 이 값을 이용해 실제 데이터가 저장된 Heap 영역에 접근한다.
- 쉽게 생각하면 인덱스 페이지만 관리하고, 실제 테이블 데이터는 따로 정렬하지 않은 상태를 유지한다고 이해할 수 있다.
- 클러스터 인덱스와 달리 여러 개의 인덱스 생성이 가능하지만, 남용하면 성능이 떨어질 수 있기 때문에 주의가 필요하다.
- 실제 데이터 페이지는 정렬되지 않으므로 데이터 INSERT, UPDATE, DELETE 작업의 속도가 빠르다.
- 인덱스만 정렬되고, 실제 데이터는 정렬되어있지 않으므로 클러스터 인덱스에 비해 검색 속도가 느리다.

# 참고 자료

- https://developers-haven.tistory.com/m/55
- [https://co-no.tistory.com/m/entry/MSSQL-인덱스-클러스터형-vs-비클러스터형](https://co-no.tistory.com/m/entry/MSSQL-%EC%9D%B8%EB%8D%B1%EC%8A%A4-%ED%81%B4%EB%9F%AC%EC%8A%A4%ED%84%B0%ED%98%95-vs-%EB%B9%84%ED%81%B4%EB%9F%AC%EC%8A%A4%ED%84%B0%ED%98%95)